spring:
  data:
    mongodb:
      database: testdb
  shell:
    command:
      version:
        enabled: false
    interactive:
      enabled: false


de:
  flapdoodle:
    mongodb:
      embedded:
        version: "7.0.5"